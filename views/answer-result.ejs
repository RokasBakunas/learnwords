<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Answer Result</title>
    <link href="/output.css" rel="stylesheet">
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
    <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
        <!--<h1 class="text-2xl font-bold text-center mb-4">Your Answer is <%= isCorrect ? 'Correct' : 'Incorrect' %></h1>-->

        <h1 class="text-2xl font-bold text-center mb-4">
            Your Answer is 
            <% if (isCorrect) { %>
                Correct <svg class="inline-block h-6 w-6 text-green-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <polyline points="20 6 9 17 4 12"></polyline>
                </svg>
            <% } else { %>
                Incorrect <svg class="inline-block h-6 w-6 text-red-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <polygon points="7.86 2 16.14 2 22 7.86 22 16.14 16.14 22 7.86 22 2 16.14 2 7.86 7.86 2" />
                    <line x1="12" y1="8" x2="12" y2="12" />
                    <line x1="12" y1="16" x2="12.01" y2="16" />
                </svg>
            <% } %>
        </h1>
        

        <p class="text-gray-700 mb-4">The correct translation for "<span class="font-semibold"><%= word.germanWord %></span>" is "<span class="font-semibold"><%= word.lithuanianWord %></span>".</p>
        <div class="text-center">
            <a href="/learn" class="inline-block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Try another word</a>
        </div>
    </div>
    <script>
        setTimeout(function() {
            window.location.href = '/learn';
        }, 3500); // 2000 milliseconds = 2 seconds
    </script>
    <script src="https://cdn.tailwindcss.com"></script>
</body>
</html>
